group 'com.luo'

// 项目依赖
dependencies {
    compile project(':shared')
}

// 项目打包
task dist(type: Zip) {
    from 'src/dist'
    into('libs') {
        from configurations.runtime
    }
}
artifacts {
    archives dist
}